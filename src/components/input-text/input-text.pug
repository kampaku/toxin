mixin input-text({label, placeholder, mask = false, type = 'text'})
  .input-text
    if label
      .input-text__title
        span.input-text__label= label
    input(class=`input-text__input ${mask && 'js-input-text__input'}` type=type placeholder=placeholder autofocus=attributes.autofocus)