mixin input-text({label, placeholder, maskContainer = false, type = 'text'})
  .input-text
    if label
      .input-text__title
        span.input-text__label= label
    input(class=`input-text__input ${maskContainer || ''}` type=type placeholder=placeholder autofocus=attributes.autofocus)